import 'package:flutter/material.dart';

class AnsweredPrayerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Answered Prayers'),
      ),
      body: Center(
        child: Text('Answered Prayer Page'),
      ),
    );
  }
}
